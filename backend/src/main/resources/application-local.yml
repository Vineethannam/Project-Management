spring:
  datasource:
    url: jdbc:h2:file:./data/projectsphere;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: ''
server:
  servlet:
    session:
      cookie:
        secure: false
