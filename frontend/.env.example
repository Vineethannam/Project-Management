# Frontend SERVER configuration; never prefix secrets with NEXT_PUBLIC_ or VITE_.
# Required. Without this backend URL, the workspace cannot sign in or load data.
SPRING_API_URL=https://project-project-management-api.onrender.com
