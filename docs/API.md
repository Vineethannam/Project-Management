# ProjectSphere API

See [complete endpoint inventory](API-ENDPOINTS.md) for every module/action URL and [routing and data architecture](ROUTING-AND-DATA.md) for payloads, pagination, permissions and frontend data flow.

The `/state` and `/entities/{kind}` endpoints have been removed. Deploy matching frontend and backend versions. Existing account, password, email and file-storage configuration remains unchanged.
